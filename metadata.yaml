# Plugin name
name: fuel-stackurity-theme
# Human-readable name for your plugin
title: Stackurity Fuel theme
# Plugin version
version: '1.0.0'
# Description
description: Installs the Stackurity theme and sets this as the default theme
# Required fuel version
fuel_version: ['9.0', '9.1','10']
# Specify license of your plugin
licenses: ['MIT']
# Specify author or company name
authors: ['FideCium']
# A link to the plugin's page
homepage: 'https://github.com/bcornelis/fuel-stackurity-theme'
# Specify a group which your plugin implements, possible options:
# network, storage, storage::cinder, storage::glance, hypervisor
groups: []
# Change `false` to `true` if the plugin can be installed in the environment
# after the deployment.
is_hotpluggable: true

# The plugin is compatible with releases in the list
releases:
  - os: ubuntu
    version: mitaka-9.0
    mode: ['ha']
    deployment_scripts_path: deployment_scripts/
    repository_path: repositories/ubuntu

# Version of plugin package
package_version: '5.0.0'
